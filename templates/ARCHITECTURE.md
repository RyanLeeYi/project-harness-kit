# ARCHITECTURE — 本次工作的邊界

> **只寫本次工作碰得到的部分，不要描述整個系統。**
>
> 理由：全貌文件沒有人維護就會過期，而 agent 會**相信**它——過期的地圖比沒有地圖更會害人。舊系統的知識改用 `docs/system-notes/`「踩到才寫」的方式累積。

**更新於**：<< YYYY-MM-DD >>

## 本次工作動到的部分

<< 資料流：輸入從哪來 → 經過什麼 → 輸出到哪。畫得下就畫，畫不下就條列 >>

## 邊界

**可以動**

| 路徑 | 是什麼 |
|------|--------|
| `<< 路徑 >>` | << 說明 >> |

**不可以動（含理由）**

| 路徑 | 為什麼不能動 |
|------|--------------|
| `<< 路徑 >>` | << 理由。說得出理由的規則才有人遵守 >> |

## 已知的耦合

<< 動這裡會影響到哪裡。這一段是 agent 最需要、也最容易漏掉的東西 >>

## 待驗證的假設

<< 你以為是這樣、但還沒確認過的事。標出來，讓接手的人知道哪裡不能盡信 >>
